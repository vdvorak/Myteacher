# Myteacher

Project overview and document map: see `README.md`.

## Agent skills

### Issue tracker

Issues and specs are GitHub Issues on `vdvorak/Myteacher`, operated with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root and ADRs in `docs/adr/`. See `docs/agents/domain.md`.
